- date_format ||= '%B %e'
%ul.articles
  - articles.each do |article|
    %li{class: "lang-#{article.data.lang}"}
      = link_to article.title, article
      - unless date_format.blank?
        %small - #{article.date.strftime(date_format)}