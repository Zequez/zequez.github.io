---
title: 'Projects'
---

.projects-index
  %h1 Projects
  %ul.projects
    - data.projects_list.each do |project|
      %li.project
        = link_to project.name, "/projects/#{project.tag}"
        %small - #{project.year}