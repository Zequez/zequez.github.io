---
title: 'Index'
---

.blog-index
  %h1 Blog
  %ul.articles
  - page_articles.each_with_index do |article, i|
    %li.article
      %h2
        = link_to article.title, article
        %span= article.date.strftime('%b %e')
.projects-index
  %h1 Projects
  %ul.projects
    %li.project
      %h2 Mapa de Transporte
.about-index
  %h1 Get In touch
  %table
    %tbody
      %tr
        %td Name
        %td Ezequiel Adrian Schwartzman
      %tr
        %td Alias
        %td Zequez

- if paginate && num_pages > 1
  %p Page #{page_number} of #{num_pages}

  - if prev_page
    %p= link_to 'Previous page', prev_page

- page_articles.each_with_index do |article, i|


  = article.body

- if paginate
  - if next_page
    %p= link_to 'Next page', next_page
