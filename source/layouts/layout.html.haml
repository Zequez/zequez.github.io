!!!
%html
  %head
    %meta{charset: "utf-8"}
    %meta{:content => "IE=edge,chrome=1", "http-equiv" => "X-UA-Compatible"}
    %title
      Zequez -
      - content_for :title, current_page.data.title || ''
      = yield_content :title
      
    / = current_page.data.title || (defined?(project) && project.name)
    = feed_tag :atom, "#{blog.options.prefix.to_s}/feed.xml", title: "Atom Feed"
    = stylesheet_link_tag "all"
    %link(rel="icon" href="/favicon.png" type="image/x-icon")
  %body{class: page_classes}
    .container
      %header.header
        %h1.author
          %a{href: '/'} Zequez
        %p.slogan Just a place to write about the stuff I make, do, think, and maybe eat
      %nav.sidebar#sidebar
        .sidebar-overlay#sidebar-overlay
        .sidebar-button#sidebar-button
        %section.sidebar-section
          %h1
            %a{href: '/projects'} Projects
          %ul
            - data.projects.each_pair do |key, project|
              %li
                %a{href: "/projects/#{project.tag}"}= project.name
          %h1
            %a{href: '/'} Latest posts
          %ul
            - blog.articles[0..7].each do |article|
              %li
                %a{href: article.url}= article.title
          %h1
            %a{href: '/about-me'} About me / Contact
          / - data.sidebar.items.each do |item|
          /   %h1
          /     - if item.url
          /       %a{href: item.url}
          /         = item.name
          /     - else
          /       = item.name
          /   - if item.items
          /     %ul
          /       - item.items.each do |subitem|
          /         %li
          /           %a{href: subitem.url}= subitem.name
      %main.main
        = yield
      %footer.footer
        Copysomethings &copy; Ezequiel A. Schwartzman, Zequez #{Time.now.year}
    = javascript_include_tag  "all"
    / %aside
    /   %h2 Recent Articles
    /   %ol
    /     - blog.articles[0...10].each do |article|
    /       %li
    /         = link_to article.title, article
    /         %span= article.date.strftime('%b %e')
    /   %h2 Tags
    /   %ol
    /     - blog.tags.each do |tag, articles|
    /       %li= link_to "#{tag} (#{articles.size})", tag_path(tag)
    /   %h2 By Year
    /   %ol
    /     - blog.articles.group_by {|a| a.date.year }.each do |year, articles|
    /       %li= link_to "#{year} (#{articles.size})", blog_year_path(year)